@model IEnumerable<SocialEyes.Domain.Entities.PollOption>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Poll.PollName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OptionIndex)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OptionText)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Poll.PollName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OptionIndex)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OptionText)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PollOptionId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PollOptionId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PollOptionId })
        </td>
    </tr>
}

</table>
